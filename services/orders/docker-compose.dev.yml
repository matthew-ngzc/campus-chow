services:
  postgres:
    image: postgres:15-alpine
    container_name: smunch_orders_pg
    restart: unless-stopped
    env_file:
      - .env
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
      TZ: ${TZ}
    ports:
      - "5434:5432" # optional; remove if you don't need host access
    volumes:
      - pgdata_orders:/var/lib/postgresql/data
    networks:
      - default     # <--- only this internal network

  adminer:
    image: adminer
    restart: unless-stopped
    ports:
      - "8094:8080"
    networks:
      - default     # <--- can reach postgres only

  order-service:
    build:
      context: .
      dockerfile: Dockerfile.dev
    command: npm run dev
    develop:
      watch:
        - action: sync
          path: ./src
          target: /app/src
          ignore:
            - node_modules/
        - action: rebuild
          path: package.json
    container_name: order-service
    env_file:
      - .env
    depends_on:
      - postgres
    ports:
      - "8084:8084"
    restart: unless-stopped
    networks:
      - default     # can reach postgres
      - smunch-network      # can reach other microservices

volumes:
  pgdata_orders:

networks:
  smunch-network:
    external: true
